vim.api.nvim_buf_set_var(0, 'type', 'code')

-- local values = vim.fn.getcompletion('b:', 'var')
-- for index, value in ipairs(values) do
-- 	print(vim.inspect(index))
-- 	print(vim.inspect(value))
-- end
